# Copy this file to .env and fill in real values. .env is gitignored.

# SQLite file lives locally — no external database needed
DATABASE_URL="file:./dev.db"

# Local API server port
PORT=4000

# Only needed once you build the document-extraction feature (Phase 4).
# Not required to run the app locally right now.
ANTHROPIC_API_KEY=
